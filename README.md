# 🚀 Telegram Multi-Owner Bot Setup Guide  

## 🛠️ 1. Install Requirements  
First, install Python and necessary packages:  
```bash
sudo apt update && sudo apt install -y python3 python3-pip docker.io docker-compose
